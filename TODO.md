# TODO

- [x] Patch `app.py` to use absolute paths based on `__file__` (model/data/reward CSV)
- [x] Add fallback response when no intent passes threshold
- [x] Validate `/get` input (handle missing/empty msg)
- [x] Validate `/reward` input (return 400 when missing)
- [x] Ensure required NLTK datasets are available at startup (download if missing)
- [x] Sanity-test prediction pipeline and Flask routes



